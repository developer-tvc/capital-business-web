import ReferralMain from '../../components/management/referral/Referral';
import SidebarLayout from '../layout/SidebarLayout';

const ReferralPage = () => {
  return (
    <SidebarLayout>
      <ReferralMain />
    </SidebarLayout>
  );
};

export default ReferralPage;
